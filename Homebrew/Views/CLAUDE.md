# Homebrew/Views/
> L2 | 父级: ../CLAUDE.md

成员清单
MainSidebarView.swift: BrewAccessibilityID + BrewFeatureInstalled + BrewUIComponents；窗口导航展示层；语言更新不触发导航状态重置。
SidebarItem.swift: BrewAccessibilityID + BrewUIComponents + SwiftUI；菜单层显式解析语言，避免依赖窗口外不可见的 locale。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
