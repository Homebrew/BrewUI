# Sources/BrewFeatureConfig/Views/
> L2 | 父级: ../CLAUDE.md

成员清单
ConfigColumnsRoot.swift: BrewAppEnvironment + BrewRepositoryInterfaces + SwiftUI；Entry-point wrapper for the Configuration tab. Reads the config repository from the environment。
ConfigSectionCard.swift: BrewUIComponents + SwiftUI；A grouped key/value card for one configuration section. Values are monospaced and selectable so the。
ConfigView.swift: AppKit + BrewAccessibilityID + BrewCore；界面展示层，不翻译包名、配置值或原始错误。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
